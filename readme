this is my first git file, dont mess with it !!! (practice lang i2 bhie)
#to stage a file 
git add <filename>
#to commit a file
git commit -m "message"
#to list status
git status

#to add remote origin 
git remote add origin https://github.com/gelaelala/testgit.git

#to push committed files from local to remote
git push -u origin <branchname>